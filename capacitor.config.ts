import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ax.reusablecomponents',
  appName: 'ax-reusable-components',
  webDir: 'out',
  server: {
    hostname: 'astrax-trail-web-bkp-erbgcwc7anhjd8hj.centralindia-01.azurewebsites.net',
    androidScheme: 'https'
  }
};

export default config;
